To reset if files accidentally added. After correcting this file

# # rm all files
# git rm -r --cached .
# # add all files as per new .gitignore
# git add .
# # now, commit for new .gitignore to apply
# git commit -m ".gitignore is now working"

.ruby_version # not used by TextMate anyway, but is it used by Nova and does it need to be tracked
# "Ruby- to backup alias"
# "Ruby-old stuff to backup alias"
# Icon\r # error if doesn't exist when commit
# "Ruby- to back up alias"
TODO
TODO.md
/currentData/photoArray.txt # git rm --cached FILENAME git rm --cached -f "currentData/photoArray.txt"


#  Following are recommended for Ruby, although I don't think I have any of these. Probably more for Ruby development itself, not using Ruby
*.gem
*.rbc
/.config
/coverage/
/InstalledFiles
/pkg/
/spec/reports/
/test/tmp/
/test/version_tmp/
/tmp/

## Documentation cache and generated files:
/.yardoc/
/_yardoc/
/doc/
/rdoc/

## Environment normalisation:
/.bundle/
/lib/bundler/man/

# for a library or gem, you might want to ignore these files since the code is
# intended to run in multiple environments; otherwise, check them in:
# Gemfile.lock
# .ruby-version
# .ruby-gemset

# unless supporting rvm < 1.11.0 or doing something fancy, ignore this:
.rvmrc

# Files used for development
/testingClass

# Files that are essentially tmp
/currentData/photoArray.txt
/currentData/photosRenamedTo.txt
